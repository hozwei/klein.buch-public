<!--
  G2-DOC.3.1 — `/help`-Indexseite.
  Der eigentliche Pfad wird vom `+page.ts`-`load()` per Redirect auf
  `/help/willkommen` abgefangen. Diese Datei rendert nur einen knappen
  Fallback, falls der Redirect ausnahmsweise nicht greift (z. B. wenn der
  Prerender später deaktiviert wird).
-->
<p>Lade Handbuch …</p>
