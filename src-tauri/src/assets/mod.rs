//! Anlagen-Modul (Phase 2C).
//! `afa_tabellen`: Loader für `inputs/specs/afa-tabellen.json`, cached in-memory.

pub mod afa_tabellen;

#[cfg(test)]
mod tests {
    #[test]
    fn it_compiles() {}
}
